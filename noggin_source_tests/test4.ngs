# test4.ngs

function empty() { }

function oneAssignment() {
	s = 1;
}

function arguments(int a, bool b, double c, var d) {
	a = b;
	# comment in the middle of two statements
	c = d;
}

function word (int one ,int two ) {
	declare int seven;
	seven = two + one ;
	do {
		word (2+5, 4);
	} while (derp == i)
	# comment at end of statements
}

function hello() {
	if (this) {
		perform(something);
	} else {
		myvar = 9;
	}
}

function empty() {
	while (something) {
		m(56);
	}
	return hello;
}
