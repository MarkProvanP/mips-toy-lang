# test4.ngs
declare function void empty();
declare function void oneAssignment();
declare function void arguments(int a, bool b, double c, var d);
declare function void word(int one, int two);
declare function void hello();
declare function void perform(int something);
declare bool something = false;
declare bool this = true;
declare int derp = 0o113;
declare function void m(int n);

function void empty() { }

function void oneAssignment() {
	s = 1;
}

function void arguments(int a, bool b, double c, var d) {
	a = b;
	# comment in the middle of two statements
	c = d;
}

function void word (int one ,int two ) {
	declare int seven;
	declare int i = 10;
	seven = two + one ;
	do {
		word (2+5, 4);
	} while (derp == i)
	# comment at end of statements
}

function void hello() {
	if (this) {
		perform(something);
	} else {
		declare int myvar = 9;
	}
}

function int empty() {
	while (something) {
		m(56);
	}
	return hello;
}
